# Production Environment Variables for Backend

# Server Configuration
PORT=5001
NODE_ENV=production

# Database Configuration (choose one option)

# Option 1: DATABASE_URL (recommended for Railway, Render, etc.)
DATABASE_URL=postgresql://username:password@host:port/database

# Option 2: Individual database variables
# DATABASE_HOST=your-db-host.com
# DATABASE_USER=your-db-user
# DATABASE_NAME=your-db-name
# DATABASE_PASSWORD=your-db-password
# DATABASE_PORT=5432

# CORS Configuration
# Add your Vercel frontend domain here
FRONTEND_URL=https://your-app.vercel.app

# File Storage (for production, consider cloud storage)
UPLOAD_DIR=uploads
ANNOTATED_IMAGES_DIR=annotated_images

# ML Models (consider storing in cloud storage)
MODELS_DIR=models

# Security
SESSION_SECRET=your-session-secret-here
JWT_SECRET=your-jwt-secret-here 